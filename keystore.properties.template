# Шаблон для keystore.properties. Скопируйте в keystore.properties
# (рядом с settings.gradle.kts) и подставьте свои значения.
# keystore.properties gitignored — в репозиторий не попадёт.
#
# Путь к .jks файлу. Относительный — от корня проекта.
storeFile=app/signing/govorun-lite.jks
storePassword=ЗАМЕНИТЕ_НА_ПАРОЛЬ_ХРАНИЛИЩА
keyAlias=govorun-lite
keyPassword=ЗАМЕНИТЕ_НА_ПАРОЛЬ_КЛЮЧА
